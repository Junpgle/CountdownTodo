# Countdown Todo 隐私政策

生效日期：2025-04-23
最近按实现核对：2026-07-20

本说明描述当前代码可识别的数据处理路径，不替代应用商店或法律审核。

## 处理的数据

- 账户与同步：用户名、认证令牌及同步所需的设备标识；服务端保存密码的
  哈希值，而不是在客户端文档中承诺可还原的明文密码存储。
- 用户内容：待办、分组、倒计时、课程、计划块、番茄钟、时间记录、设置、
  协作内容和冲突/审计数据。
- 可选能力：Android 屏幕使用时长、系统日历事件、通知、剪贴板链接、
  音乐状态、桌面/状态栏展示和小米手环同步。仅在相应功能启用且平台允许时
  访问。
- AI：用户主动提交给 AI 功能的文本、上下文和所配置服务所需的数据；实际
  处理方取决于用户选择的模型服务。

## 存储与传输

- 业务数据首先保存在本机 SQLite/偏好设置中；登录会话令牌目前保存在应用
  偏好存储中。
- Android 和 Windows 原生客户端按环境直接访问阿里云 HTTP/WebSocket
  服务；Flutter Web 通过 `https://api-cdt.junpgle.me/` 的 Cloudflare Zero
  Trust 入口访问。
- 用户侧 Cloudflare Worker 服务器选项已停用，但 Cloudflare 仍用于 Web
  代理和 Turnstile 人机验证；仓库中的 Worker 代码仍为兼容实现。
- 当前原生网络层的部分迁移、局域网及通用 HTTP 辅助代码允许无效证书。
  因此不应把所有原生传输描述为已强制证书校验；这是待整改的安全风险。

## 权限用途

- 通知/闹钟/后台执行：发送待办、倒计时、课程、计划块和番茄钟提醒。
- 日历：在用户开启同步后创建、更新或删除对应的系统日历事件。
- 使用情况访问：仅用于用户主动启用的屏幕使用统计。
- 网络：账户、同步、协作、更新、AI 和手环通信。
- 剪贴板、媒体或辅助展示：仅用于对应的平台岛、状态栏或快捷功能。

## 用户控制

用户可在应用内关闭可选同步或平台能力、导出/导入本地数据、退出登录，
并可通过项目公布的支持渠道请求处理账户和云端数据。卸载应用通常会移除
应用沙盒中的本地数据，但系统日历、导出文件或外部服务中的副本需在相应
位置另行处理。

我们不会把用户内容作为广告数据出售。第三方 AI、Cloudflare、系统日历、
应用商店及云服务可能适用各自的隐私条款。隐私实现或服务拓扑变化时，应
同步更新本文件。
