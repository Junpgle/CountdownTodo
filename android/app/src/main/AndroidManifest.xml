<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- 权限声明 -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.POST_PROMOTED_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE"/>
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>
    <!-- 精确闹钟：保活核心权限，Android 12+ 需要用户在设置中授权 -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>
    <!-- 开机自启：重新注册提醒 Alarm -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <!-- 存储权限：用于 Android 10 及以下版本 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="29" />

    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" tools:ignore="ScopedStorage" />

    <uses-permission android:name="moe.shizuku.manager.permission.API_V23" />
    <queries>
        <package android:name="moe.shizuku.privileged.api" />
    </queries>

    <uses-permission
        android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>

    <application
        android:label="CountDownTodo"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"
                />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- ========================================== -->
            <!-- 🚀 新增：处理通过“分享”发送到本应用的 JSON 文件 -->
            <!-- ========================================== -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="application/json" />
                <data android:mimeType="text/plain" />
                <data android:mimeType="*/*" /> <!-- 兜底不规范的 MIME 类型 -->
            </intent-filter>

            <!-- ========================================== -->
            <!-- 🚀 新增：处理在文件管理器点击“打开方式”为本应用的 JSON 文件 -->
            <!-- ========================================== -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="file" />
                <data android:scheme="content" />
                <data android:host="*" />
                <!-- 匹配 .json 和 .txt 文件后缀 -->
                <data android:pathPattern=".*\\.json" />
                <data android:pathPattern=".*\\.txt" />
                <!-- 由于部分文件管理器的 MIME 类型五花八门，这里用 */* 配合 pathPattern 使用 -->
                <data android:mimeType="*/*" />
            </intent-filter>

        </activity>

        <provider
            android:name="vn.hunghd.flutterdownloader.DownloadedFileProvider"
            android:authorities="${applicationId}.flutter_downloader.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths"/>
        </provider>

        <!-- ========================================== -->
        <!-- 🚀 保活：精确 Alarm 提醒 Service             -->
        <!-- ========================================== -->
        <service
            android:name=".ReminderService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="reminder"/>
        </service>

        <receiver
            android:name=".ReminderAlarmReceiver"
            android:exported="true">
            <intent-filter>
                <!-- 精确 Alarm 触发（内部广播，包名限定，不对外暴露） -->
                <action android:name="com.math_quiz.ACTION_REMINDER_FIRE"/>
                <!-- 开机后重新注册 Alarm（需要 exported=true 接收系统广播） -->
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <!-- App 更新后重新注册 Alarm -->
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
            </intent-filter>
        </receiver>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- ========================================== -->
        <!-- 🚀 注册桌面小部件 Receiver 并增加自定义 Action -->
        <!-- ========================================== -->
        <receiver
            android:name=".TodoWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="MARK_TODO_DONE" /> <!-- 必须有这个拦截器才能生效 -->
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_todo_info" />
        </receiver>
        <service
            android:name=".TodoWidgetService"
            android:permission="android.permission.BIND_REMOTEVIEWS" />

    </application>
</manifest>